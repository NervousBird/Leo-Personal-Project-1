export interface UserData extends UserDataObject {
  id: number
}

export interface UserDataObject {
  colors: string
  borders: string
  fonts: string
  datesRange: string
  leavingPoint: string
  userId: number
}
